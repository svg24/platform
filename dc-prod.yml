version: '3.8'

services:
  nginx:
    build:
      args:
        - IS_PROD=true
    volumes:
      - dhparam:/etc/ssl/certs
      - svg24_certbot-etc:/etc/letsencrypt
      - svg24_certbot-lib:/var/lib/letsencrypt
    ports:
      - '443:443'

volumes:
  dhparam:
    driver: local
    driver_opts:
      type: none
      device: /etc/ssl/certs
      o: bind
  svg24_certbot-etc:
    external: true
  svg24_certbot-lib:
    external: true
