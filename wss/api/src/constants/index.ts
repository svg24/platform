export const SCHEMA_STRING_REQUIRED = {
  type: String,
  required: true,
};

export enum MODEL_NAMES {
  LOGO = 'Logo',
}

export enum ROUTER_PATHS {
  HOME = '/',
}

export enum DB_FILES {
  INFO = 'info'
}
